# StudyGenie Environment Variables
# Copy this file to .env and fill in your API keys

# AI is now powered by Puter.js (free, no API key required)
# Puter.js provides GPT-4o, Claude, and other AI models for free

# Hugging Face API Key (optional - app runs in mock mode without it)
# Get your key from: https://huggingface.co/settings/tokens
HF_API_KEY=your_huggingface_api_key_here

# Database Configuration (automatically set by Docker Compose)
DATABASE_URL=postgresql://studygenie_user:studygenie_pass@postgres:5432/studygenie

# JWT Secret (change this in production)
JWT_SECRET=your-jwt-secret-key-change-in-production

# Node Environment
NODE_ENV=development

# API URL for frontend (automatically set by Docker Compose)
VITE_API_URL=http://localhost:5000
